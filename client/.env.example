# API Configuration
# Leave empty to use proxy in development
# Set to your backend URL in production (e.g., https://api.yoursite.com)
VITE_API_URL=

# Dev proxy target (used by Vite dev server when VITE_API_URL is relative)
VITE_API_PROXY_TARGET=http://localhost:5501

# Cloudinary (not needed - handled by backend)
# These are kept for backward compatibility but should not be used
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
